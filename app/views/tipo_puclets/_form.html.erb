<%= form_with(model: tipo_puclet, local: true) do |form| %>
  <% if tipo_puclet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_puclet.errors.count, "error") %> prohibited this tipo_puclet from being saved:</h2>

      <ul>
      <% tipo_puclet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :tipo_puclet_nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
