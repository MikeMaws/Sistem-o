<%= form_with(model: material_didatico, local: true) do |form| %>
  <% if material_didatico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(material_didatico.errors.count, "error") %> prohibited this material_didatico from being saved:</h2>

      <ul>
      <% material_didatico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, id: :material_didatico_titulo %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :material_didatico_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
