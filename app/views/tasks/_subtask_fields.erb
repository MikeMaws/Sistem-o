
      <div class="panel-heading">
        <h4>SubTarefa</h4>
      </div>
      <div class="panel-body">
          <div class="form-group">
             <%= f.label :nome %>
             <%= f.text_field :nome, autofocus: true, placeholder: "Nome da tarefa",class: 'form-control' %>
          </div>
          <div class="form-group">
             <%= f.label :descricao %>
             <%= f.text_area :descricao,placeholder: "Digite a descricão da tarefa",class: 'form-control' %>
          </div>
          <div class="form-group">
              <%= f.label :avaliativa %>
              <%= f.check_box :avaliativa %>
          </div>

          <p/>
          <div class="form-group">
              <%= f.label :entrega %>
              <%= f.date_select(:entrega,:start_year => 1950) %>
          </div>

          <div class="form-group">
            <%= f.fields_for :puclets do |f| %>
                <%=render 'puclet_fields', f:f%>
            <%end%>
          </div>

          <div class="form-group">
            <%= link_to_add_puclets "Adicionar Puclet", f, :puclets, 'add_puclets'%>
          </div>

          <div class="form-group">
            <%= f.fields_for :materialDidatico do |f| %>
                <%=render 'materialDidatico_fields', f:f%>
            <%end%>
          </div>

          <div class="form-group">
            <%= link_to_add_puclets "Adicionar Material Didatico", f, :materialDidatico, 'add_material_didatico'%>
          </div>
      </div>
